%h2 Registrarse al newsletter
- form_for(newsletter_registration) do |f|
  = f.error_messages
  %br
    = f.label :email
  %br
    = f.text_field :email
  %br
  %br  
    = f.submit 'Registrarse'
.clear

=render :partial => "/newsletter_registrations/js_validation", :locals => { :form_id => "new_newsletter_registration" }