#sub_header
  =link_to(image_tag("new/btn_contar_nueva_historia.png"), new_story_path)

#sidebar
  #header
  #sb_content
    %ul
      -Category.all.each do |category|
        %li=link_to(category.name + " (#{category.stories.size})", category_path(category))

#content
  #top
    test
  #middle
    #stories
      =render :partial => 'stories/story_new', :collection => @stories