-title("queputamierda.com - Lee que mierda pasó a la gente")

.grid_16
  .grid_10.alpha
    .top_round_section
    =render :partial => 'stories/story', :collection => @stories
  
  .sidebar    
    .grid_5
      -if logged_in?
        .round_section
          %h2="¡Hola #{current_user.login}!"
          Estas conectado
          %br
          %br
          =link_to "Cerrar sesión", logout_path

      .round_section
        =render :partial => "/categories/categories"

      -unless logged_in?
        .round_section
          =render :partial => "/sessions/login"
