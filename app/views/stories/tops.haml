-title("Las putadas mas grandes")

.grid_16
  .grid_10.alpha
    .top_round_section
    =render :partial => 'stories/story', :collection => @stories
  
  .sidebar    
    .grid_5
      -if logged_in?
        .round_section
          %h2="¡Hola #{current_user.login}!"
          Estas connectado

      .round_section
        =render :partial => "/categories/categories"

      -unless logged_in?
        .round_section
          =render :partial => "/sessions/login"
