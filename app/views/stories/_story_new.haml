.story{:id => dom_id(story_new)}
  .left
    =comment_bubble_link(story_new)
  .middle
    %h2
      =link_to(story_new.title, story_path(story_new))
      =category_as_tag(story_new)
    
    .text
      =truncate(story_new.body, 400).strip
      =link_to("(Leer mas)", story_path(story_new), :class => "leer_mas", :rel => "nofollow")

    .story_footer
      .flag
        =image_tag "new/flags/#{story_new.country_code}.png"
      .text
        ="Escrito por: #{author(story_new)} el #{my_date(story_new.created_at)}"
    
    
    
    / .footer
    /   .category.fl
    /     ="#{link_to(story_new.category.name, category_path(story_new.category))}/#{my_date(story_new.created_at)}"
    /   .vote_top.fl
    /     %span
    /       -unless session[:flop_votes].include?(story_new.id) or session[:top_votes].include?(story_new.id)
    /         =link_to_function "¡Qué puta mierda!(#{story_new.rated_top})", "vote_story(#{story_new.id}, 'top', 'collection')", :class => "vote_link"
    /       -else
    /         =link_to_function "¡Qué puta mierda!(#{story_new.rated_top})", "alert('Ya hay un voto tuyo para esta historia')", :class => "inactive"
    /   .vote_flop.fl
    /     %span
    /       -unless session[:flop_votes].include?(story_new.id) or session[:top_votes].include?(story_new.id)
    /         =link_to_function "Te lo buscaste(#{story_new.rated_flop})", "vote_story(#{story_new.id}, 'flop', 'collection')", :class => "vote_link"
    /       -else
    /         =link_to_function "Te lo buscaste(#{story_new.rated_flop})", "alert('Ya hay un voto tuyo para esta historia')", :class => "inactive"
    /   .comments
    /     =link_to("Comentarios(#{story_new.comments.size})", story_path(story, :anchor => "comments"))
  .divider &nbsp;
  .right
    .thumbs
      =thumbs_up_link(story_new)
    .thumbs      
      =thumbs_down_link(story_new)
  .footer.cl
    =image_tag "new/footer_story_divider.png"